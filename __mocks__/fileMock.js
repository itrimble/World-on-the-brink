// __mocks__/fileMock.js
// This mock is used for any file imports that Jest can't handle by default (e.g., images, stylesheets if not using identity-obj-proxy for CSS).
// It simply returns a string (like 'test-file-stub') for these assets.
module.exports = 'test-file-stub';
```
